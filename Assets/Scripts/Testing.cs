using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Créer une classe pour une pomme
// une pomme a une couleur et un poids (en grammes, environ 100g)
// => créer plusieurs pommes et afficher leur couleur et leur poids
// Indication : on peut tout à fait créer une méthode qui écrit directement dans la console

// Pensez à remplacer Testing par le nom de votre script pour que ça fonctionne
public class Testing : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Créer les pommes ici et afficher leurs propriétés
    }

    // Update is called once per frame
    void Update()
    {
    }
}
